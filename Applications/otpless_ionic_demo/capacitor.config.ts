import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ionic.demo',
  appName: 'otpless_ionic_demo',
  webDir: 'build',
  server: {
    androidScheme: 'https'
  }
};

export default config;
